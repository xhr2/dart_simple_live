name: app-build-action-dev-windows

on:
  workflow_dispatch:
  push:
    tags:
      - "dev_v*"

jobs:
  build-windows:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      # 1. Checkout 代码
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      # 2. 设置 Flutter
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          # 请根据您的项目需要，设置精确的 Flutter 版本
          flutter-version: "3.38.x"
          cache: true

      # 3. 启用 Windows Desktop
      - name: Enable Flutter Desktop
        run: flutter config --enable-windows-desktop

      # 4. 更新依赖
      - name: Restore Packages
        run: flutter pub get
        working-directory: simple_live_app # 在 simple_live_app 目录下执行

      # 5. 安装 flutter_distributor
      - name: Install flutter_distributor
        run: dart pub global activate flutter_distributor

      # 6. 构建 Windows MSIX 和 ZIP
      # 注意：如果 MSIX 构建失败（缺少签名），这里只会生成 ZIP。
      - name: Build Windows
        run: flutter_distributor package --platform windows --targets msix,zip --skip-clean
        working-directory: simple_live_app # 在 simple_live_app 目录下执行

      # 7. 打印 build 目录结构 (用于调试上传路径的关键步骤)
      - name: Show build output and Verify Files
        run: |
          echo "======= Windows build output ======="
          # 递归列出 simple_live_app/build/ 目录下的所有内容
          ls build -Recurse
          
          echo "--- Attempting to find MSIX/ZIP files ---"
          # 使用 Get-ChildItem 查找 .msix 和 .zip 文件
          Get-ChildItem -Path ".\build" -Filter "*.msix" -Recurse -ErrorAction SilentlyContinue
          Get-ChildItem -Path ".\build" -Filter "*.zip" -Recurse -ErrorAction SilentlyContinue
        working-directory: simple_live_app

      # 8. 上传产物到 GitHub Artifacts
      # 路径修正：假定产物在 build/dist/windows/ 或其子目录，使用 ** 匹配
      - name: Upload Windows APP to Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows
          # 修正后的路径，** 确保可以找到 dist/windows/ 下的任一位置的文件
          path: |
            simple_live_app/build/**/*.msix
            simple_live_app/build/**/*.zip 

      # 9. 输出 job 状态
      - name: Print Job Status
        run: echo "🍏 Windows job's status is ${{ job.status }}."
